rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access to portfolio site content
    match /portfolio/{document=**} {
      allow read, write: if true;
    }
  }
}
