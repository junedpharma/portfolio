rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read and write access for hero images, article images, and notice PDFs
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
